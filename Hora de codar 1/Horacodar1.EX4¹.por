programa {
  funcao inicio() {
    real altura, base, area

    escreva("Qual é a altura? ")
    leia(altura)

    escreva("Qual é a base? ")
    leia(base)

    area = (base* altura)
    escreva("A área é: ", area)

    

    
  }
}
